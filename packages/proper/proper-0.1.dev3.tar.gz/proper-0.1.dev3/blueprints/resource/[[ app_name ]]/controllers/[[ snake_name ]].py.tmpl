from .application import ApplicationController


class [[ class_name|safe ]](ApplicationController):
    [% if  "index" in actions -%]
    def index(self, req, resp):
        pass
    [%- endif %]

    [% if  "new" in actions -%]
    def new(self, req, resp):
        pass
    [%- endif %]

    [% if  "create" in actions -%]
    def create(self, req, resp):
        pass
    [%- endif %]

    [% if  "show" in actions -%]
    def show(self, req, resp[% if not singular %], uid[% endif %]):
        pass
    [%- endif %]

    [% if  "edit" in actions -%]
    def edit(self, req, resp[% if not singular %], uid[% endif %]):
        pass
    [%- endif %]

    [% if  "update" in actions -%]
    def update(self, req, resp[% if not singular %], uid[% endif %]):
        pass
    [%- endif %]

    [% if  "delete" in actions -%]
    def delete(self, req, resp[% if not singular %], uid[% endif %]):
        pass
    [%- endif %]
