import os


debug = False
host = ""

# databases = {
#     "default": "main",
#     "main": {
#         "driver": "postgres",
#         "host": os.environ.get("POSTGRES_DATABASE_HOST", "localhost"),
#         "database": os.environ.get("POSTGRES_DATABASE_NAME", "[[ app_name ]]"),
#         "user": os.environ.get("POSTGRES_DATABASE_USER", "postgres"),
#         "password": os.environ.get("POSTGRES_DATABASE_PASSWORD", ""),
#     },
#     "tests": {
#         "driver": "postgres",
#         "host": os.environ.get("POSTGRES_DATABASE_HOST", "localhost"),
#         "database": os.environ.get("POSTGRES_DATABASE_NAME", "[[ app_name ]]_tests"),
#         "user": os.environ.get("POSTGRES_DATABASE_USER", "postgres"),
#         "password": os.environ.get("POSTGRES_DATABASE_PASSWORD", ""),
#     },
# }
databases = {
    "default": "main",
    "main": {
        "driver": "sqlite",
        "database": "db/dev.sqlite",
    },
    "tests": {
        "driver": "sqlite",
        "database": "db/tests.sqlite",
    },
}

auth = {
    "hash_name": "argon2",
    "rounds": None,
    "password_minlen": 9,
    "password_maxlen": 1024,
    "token_life": 10800,  # 3 hours
}

mailer = {
    "default_from": "admin@example.com",
}

session = {
    "cookie_name": "_session",
    "cookie_httponly": True,
}

redis = {
    "host": os.environ.get("REDIS_HOST", "localhost"),
    "port": 6379,
    "db": 0,
}
