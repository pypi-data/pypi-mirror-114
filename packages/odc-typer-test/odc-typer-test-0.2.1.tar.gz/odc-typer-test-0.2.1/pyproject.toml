[tool.poetry]
name = "odc-typer-test"
version = "0.2.1"
description = "This is my package that tests out Typer."
authors = ["one-data-cookie <kolacek.m@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
odc-typer-test = "odc_typer_test.main:app"

[tool.poetry.dependencies]
python = "^3.8"
typer = "^0.3.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
