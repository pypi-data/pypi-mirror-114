from setuptools import setup

setup(name='probability_distributions_egv',
        version='1.1',
        description='probability distributions_egv',
      packages=['probability_distributions_egv'],
      zip_safe=False)