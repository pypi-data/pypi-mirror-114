CONFIG = {
    "config": {
        "default": None,
        "help": "Load extra options from a configuration file onto hub.OPT.idem_aiohttp",
    }
}

SUBCOMMANDS = {}

CLI_CONFIG = {
    "config": {"options": ["-c"]},
}

DYNE = {"tool": ["tool"], "exec": ["exec"], "acct": ["acct"]}
