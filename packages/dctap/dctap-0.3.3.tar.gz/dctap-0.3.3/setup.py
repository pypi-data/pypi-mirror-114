#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['dctap']

package_data = \
{'': ['*'],
 'dctap': ['.mypy_cache/*',
           '.mypy_cache/3.9/*',
           '.mypy_cache/3.9/_typeshed/*',
           '.mypy_cache/3.9/collections/*',
           '.mypy_cache/3.9/importlib/*',
           '.mypy_cache/3.9/os/*']}

install_requires = \
['click', 'dataclasses', 'ruamel.base', 'ruamel.yaml']

extras_require = \
{'doc': ['sphinx', 'sphinx_rtd_theme'], 'test': ['pytest', 'pytest-cov']}

entry_points = \
{'console_scripts': ['dctap = dctap.cli:cli']}

setup(name='dctap',
      version='0.3.3',
      description='Validate and debug DCTAP instances.',
      author='DCMI',
      author_email='admin@dublincore.net',
      url='https://github.com/dcmi/dctap-python',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.7',
     )
