class WordLengthInList:

    def __init__(self, check):
        self.check = check

    def check(list):
        word_length_list = []
        if list is list:
            for word in list:
                word_length = len(word)
                word_length_list.append(word_length)
            print(word_length_list)
        
        