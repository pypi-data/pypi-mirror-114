"""
    Lethean VPN

    Distributed Virtual Private Marketplace  # noqa: E501

    The version of the OpenAPI document: 1.0.7
    Contact: contact@lethean.io
    Generated by: https://openapi-generator.tech
"""


import re  # noqa: F401
import sys  # noqa: F401

from lethean.model_utils import (  # noqa: F401
    ApiTypeError,
    ModelComposed,
    ModelNormal,
    ModelSimple,
    cached_property,
    change_keys_js_to_python,
    convert_js_args_to_python_args,
    date,
    datetime,
    file_type,
    none_type,
    validate_get_composed_info,
)
from ..model_utils import OpenApiModel
from lethean.exceptions import ApiAttributeError



class NetworkStatsEntity(ModelNormal):
    """NOTE: This class is auto generated by OpenAPI Generator.
    Ref: https://openapi-generator.tech

    Do not edit the class manually.

    Attributes:
      allowed_values (dict): The key is the tuple path to the attribute
          and the for var_name this is (var_name,). The value is a dict
          with a capitalized key describing the allowed value and an allowed
          value. These dicts store the allowed enum values.
      attribute_map (dict): The key is attribute name
          and the value is json key in definition.
      discriminator_value_class_map (dict): A dict to go from the discriminator
          variable value to the discriminator class name.
      validations (dict): The key is the tuple path to the attribute
          and the for var_name this is (var_name,). The value is a dict
          that stores validations for max_length, min_length, max_items,
          min_items, exclusive_maximum, inclusive_maximum, exclusive_minimum,
          inclusive_minimum, and regex.
      additional_properties_type (tuple): A tuple of classes accepted
          as additional properties values.
    """

    allowed_values = {
    }

    validations = {
    }

    @cached_property
    def additional_properties_type():
        """
        This must be a method because a model may have properties that are
        of type self, this must run after the class is loaded
        """
        return (bool, date, datetime, dict, float, int, list, str, none_type,)  # noqa: E501

    _nullable = False

    @cached_property
    def openapi_types():
        """
        This must be a method because a model may have properties that are
        of type self, this must run after the class is loaded

        Returns
            openapi_types (dict): The key is attribute name
                and the value is attribute type.
        """
        return {
            'alt_blocks_count': (float,),  # noqa: E501
            'block_size_limit': (float,),  # noqa: E501
            'cumulative_difficulty': (float,),  # noqa: E501
            'current': (bool,),  # noqa: E501
            'difficulty': (float,),  # noqa: E501
            'fee_per_kb': (float,),  # noqa: E501
            'grey_peerlist_size': (float,),  # noqa: E501
            'hash_rate': (float,),  # noqa: E501
            'height': (float,),  # noqa: E501
            'incoming_connections_count': (float,),  # noqa: E501
            'outgoing_connections_count': (float,),  # noqa: E501
            'start_time': (float,),  # noqa: E501
            'status': (bool,),  # noqa: E501
            'target': (float,),  # noqa: E501
            'target_height': (float,),  # noqa: E501
            'testnet': (bool,),  # noqa: E501
            'top_block_hash': (str,),  # noqa: E501
            'tx_count': (float,),  # noqa: E501
            'tx_pool_size': (float,),  # noqa: E501
            'tx_pool_size_kbytes': (float,),  # noqa: E501
            'white_peerlist_size': (float,),  # noqa: E501
        }

    @cached_property
    def discriminator():
        return None


    attribute_map = {
        'alt_blocks_count': 'alt_blocks_count',  # noqa: E501
        'block_size_limit': 'block_size_limit',  # noqa: E501
        'cumulative_difficulty': 'cumulative_difficulty',  # noqa: E501
        'current': 'current',  # noqa: E501
        'difficulty': 'difficulty',  # noqa: E501
        'fee_per_kb': 'fee_per_kb',  # noqa: E501
        'grey_peerlist_size': 'grey_peerlist_size',  # noqa: E501
        'hash_rate': 'hash_rate',  # noqa: E501
        'height': 'height',  # noqa: E501
        'incoming_connections_count': 'incoming_connections_count',  # noqa: E501
        'outgoing_connections_count': 'outgoing_connections_count',  # noqa: E501
        'start_time': 'start_time',  # noqa: E501
        'status': 'status',  # noqa: E501
        'target': 'target',  # noqa: E501
        'target_height': 'target_height',  # noqa: E501
        'testnet': 'testnet',  # noqa: E501
        'top_block_hash': 'top_block_hash',  # noqa: E501
        'tx_count': 'tx_count',  # noqa: E501
        'tx_pool_size': 'tx_pool_size',  # noqa: E501
        'tx_pool_size_kbytes': 'tx_pool_size_kbytes',  # noqa: E501
        'white_peerlist_size': 'white_peerlist_size',  # noqa: E501
    }

    read_only_vars = {
    }

    _composed_schemas = {}

    @classmethod
    @convert_js_args_to_python_args
    def _from_openapi_data(cls, alt_blocks_count, block_size_limit, cumulative_difficulty, current, difficulty, fee_per_kb, grey_peerlist_size, hash_rate, height, incoming_connections_count, outgoing_connections_count, start_time, status, target, target_height, testnet, top_block_hash, tx_count, tx_pool_size, tx_pool_size_kbytes, white_peerlist_size, *args, **kwargs):  # noqa: E501
        """NetworkStatsEntity - a model defined in OpenAPI

        Args:
            alt_blocks_count (float):
            block_size_limit (float):
            cumulative_difficulty (float):
            current (bool):
            difficulty (float):
            fee_per_kb (float):
            grey_peerlist_size (float):
            hash_rate (float):
            height (float):
            incoming_connections_count (float):
            outgoing_connections_count (float):
            start_time (float):
            status (bool):
            target (float):
            target_height (float):
            testnet (bool):
            top_block_hash (str):
            tx_count (float):
            tx_pool_size (float):
            tx_pool_size_kbytes (float):
            white_peerlist_size (float):

        Keyword Args:
            _check_type (bool): if True, values for parameters in openapi_types
                                will be type checked and a TypeError will be
                                raised if the wrong type is input.
                                Defaults to True
            _path_to_item (tuple/list): This is a list of keys or values to
                                drill down to the model in received_data
                                when deserializing a response
            _spec_property_naming (bool): True if the variable names in the input data
                                are serialized names, as specified in the OpenAPI document.
                                False if the variable names in the input data
                                are pythonic names, e.g. snake case (default)
            _configuration (Configuration): the instance to use when
                                deserializing a file_type parameter.
                                If passed, type conversion is attempted
                                If omitted no type conversion is done.
            _visited_composed_classes (tuple): This stores a tuple of
                                classes that we have traveled through so that
                                if we see that class again we will not use its
                                discriminator again.
                                When traveling through a discriminator, the
                                composed schema that is
                                is traveled through is added to this set.
                                For example if Animal has a discriminator
                                petType and we pass in "Dog", and the class Dog
                                allOf includes Animal, we move through Animal
                                once using the discriminator, and pick Dog.
                                Then in Dog, we will make an instance of the
                                Animal class but this time we won't travel
                                through its discriminator because we passed in
                                _visited_composed_classes = (Animal,)
        """

        _check_type = kwargs.pop('_check_type', True)
        _spec_property_naming = kwargs.pop('_spec_property_naming', False)
        _path_to_item = kwargs.pop('_path_to_item', ())
        _configuration = kwargs.pop('_configuration', None)
        _visited_composed_classes = kwargs.pop('_visited_composed_classes', ())

        self = super(OpenApiModel, cls).__new__(cls)

        if args:
            raise ApiTypeError(
                "Invalid positional arguments=%s passed to %s. Remove those invalid positional arguments." % (
                    args,
                    self.__class__.__name__,
                ),
                path_to_item=_path_to_item,
                valid_classes=(self.__class__,),
            )

        self._data_store = {}
        self._check_type = _check_type
        self._spec_property_naming = _spec_property_naming
        self._path_to_item = _path_to_item
        self._configuration = _configuration
        self._visited_composed_classes = _visited_composed_classes + (self.__class__,)

        self.alt_blocks_count = alt_blocks_count
        self.block_size_limit = block_size_limit
        self.cumulative_difficulty = cumulative_difficulty
        self.current = current
        self.difficulty = difficulty
        self.fee_per_kb = fee_per_kb
        self.grey_peerlist_size = grey_peerlist_size
        self.hash_rate = hash_rate
        self.height = height
        self.incoming_connections_count = incoming_connections_count
        self.outgoing_connections_count = outgoing_connections_count
        self.start_time = start_time
        self.status = status
        self.target = target
        self.target_height = target_height
        self.testnet = testnet
        self.top_block_hash = top_block_hash
        self.tx_count = tx_count
        self.tx_pool_size = tx_pool_size
        self.tx_pool_size_kbytes = tx_pool_size_kbytes
        self.white_peerlist_size = white_peerlist_size
        for var_name, var_value in kwargs.items():
            if var_name not in self.attribute_map and \
                        self._configuration is not None and \
                        self._configuration.discard_unknown_keys and \
                        self.additional_properties_type is None:
                # discard variable.
                continue
            setattr(self, var_name, var_value)
        return self

    required_properties = set([
        '_data_store',
        '_check_type',
        '_spec_property_naming',
        '_path_to_item',
        '_configuration',
        '_visited_composed_classes',
    ])

    @convert_js_args_to_python_args
    def __init__(self, alt_blocks_count, block_size_limit, cumulative_difficulty, current, difficulty, fee_per_kb, grey_peerlist_size, hash_rate, height, incoming_connections_count, outgoing_connections_count, start_time, status, target, target_height, testnet, top_block_hash, tx_count, tx_pool_size, tx_pool_size_kbytes, white_peerlist_size, *args, **kwargs):  # noqa: E501
        """NetworkStatsEntity - a model defined in OpenAPI

        Args:
            alt_blocks_count (float):
            block_size_limit (float):
            cumulative_difficulty (float):
            current (bool):
            difficulty (float):
            fee_per_kb (float):
            grey_peerlist_size (float):
            hash_rate (float):
            height (float):
            incoming_connections_count (float):
            outgoing_connections_count (float):
            start_time (float):
            status (bool):
            target (float):
            target_height (float):
            testnet (bool):
            top_block_hash (str):
            tx_count (float):
            tx_pool_size (float):
            tx_pool_size_kbytes (float):
            white_peerlist_size (float):

        Keyword Args:
            _check_type (bool): if True, values for parameters in openapi_types
                                will be type checked and a TypeError will be
                                raised if the wrong type is input.
                                Defaults to True
            _path_to_item (tuple/list): This is a list of keys or values to
                                drill down to the model in received_data
                                when deserializing a response
            _spec_property_naming (bool): True if the variable names in the input data
                                are serialized names, as specified in the OpenAPI document.
                                False if the variable names in the input data
                                are pythonic names, e.g. snake case (default)
            _configuration (Configuration): the instance to use when
                                deserializing a file_type parameter.
                                If passed, type conversion is attempted
                                If omitted no type conversion is done.
            _visited_composed_classes (tuple): This stores a tuple of
                                classes that we have traveled through so that
                                if we see that class again we will not use its
                                discriminator again.
                                When traveling through a discriminator, the
                                composed schema that is
                                is traveled through is added to this set.
                                For example if Animal has a discriminator
                                petType and we pass in "Dog", and the class Dog
                                allOf includes Animal, we move through Animal
                                once using the discriminator, and pick Dog.
                                Then in Dog, we will make an instance of the
                                Animal class but this time we won't travel
                                through its discriminator because we passed in
                                _visited_composed_classes = (Animal,)
        """

        _check_type = kwargs.pop('_check_type', True)
        _spec_property_naming = kwargs.pop('_spec_property_naming', False)
        _path_to_item = kwargs.pop('_path_to_item', ())
        _configuration = kwargs.pop('_configuration', None)
        _visited_composed_classes = kwargs.pop('_visited_composed_classes', ())

        if args:
            raise ApiTypeError(
                "Invalid positional arguments=%s passed to %s. Remove those invalid positional arguments." % (
                    args,
                    self.__class__.__name__,
                ),
                path_to_item=_path_to_item,
                valid_classes=(self.__class__,),
            )

        self._data_store = {}
        self._check_type = _check_type
        self._spec_property_naming = _spec_property_naming
        self._path_to_item = _path_to_item
        self._configuration = _configuration
        self._visited_composed_classes = _visited_composed_classes + (self.__class__,)

        self.alt_blocks_count = alt_blocks_count
        self.block_size_limit = block_size_limit
        self.cumulative_difficulty = cumulative_difficulty
        self.current = current
        self.difficulty = difficulty
        self.fee_per_kb = fee_per_kb
        self.grey_peerlist_size = grey_peerlist_size
        self.hash_rate = hash_rate
        self.height = height
        self.incoming_connections_count = incoming_connections_count
        self.outgoing_connections_count = outgoing_connections_count
        self.start_time = start_time
        self.status = status
        self.target = target
        self.target_height = target_height
        self.testnet = testnet
        self.top_block_hash = top_block_hash
        self.tx_count = tx_count
        self.tx_pool_size = tx_pool_size
        self.tx_pool_size_kbytes = tx_pool_size_kbytes
        self.white_peerlist_size = white_peerlist_size
        for var_name, var_value in kwargs.items():
            if var_name not in self.attribute_map and \
                        self._configuration is not None and \
                        self._configuration.discard_unknown_keys and \
                        self.additional_properties_type is None:
                # discard variable.
                continue
            setattr(self, var_name, var_value)
            if var_name in self.read_only_vars:
                raise ApiAttributeError(f"`{var_name}` is a read-only attribute. Use `from_openapi_data` to instantiate "
                                     f"class with read only attributes.")
