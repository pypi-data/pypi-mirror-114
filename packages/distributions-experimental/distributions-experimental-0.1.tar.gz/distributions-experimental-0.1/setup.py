from setuptools import setup

setup(name='distributions-experimental',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions-experimental'],
      zip_safe=False)
